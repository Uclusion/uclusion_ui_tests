name: Remove Stage Users

on:
  release:
    types: [published]

jobs:
  build:
    if: startsWith(github.event.release.tag_name, 'stage_specific')
    uses: ./.github/workflows/reusableRemoveSpecificUsers.yml
    secrets: inherit
    with:
      emails: 'testeruclusion@gmail.com'
      environment: "stage"
